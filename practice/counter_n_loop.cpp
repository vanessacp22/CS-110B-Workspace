#include <iostream>
using namespace std;

int main() {
    int rowNum; // The number of rows to display
    int rowCount = 0; // Counter to keep track of the current number in the row

    cout << "Enter a number to display it's number in rows: " << endl; // Prompts the user to enter a number
    cin >> n; // Accepts user input and stores it in variable n

    for(int i = 1; i <= rowNum; i++){
        for()
    }
}